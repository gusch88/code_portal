# Supabase Credentials
# Get these from: https://app.supabase.com → Settings → API

SUPABASE_URL=
SUPABASE_ANON=

# Nur für import-bestand.js benötigt (Settings → API → service_role).
# Niemals im Client-Code verwenden oder committen!
SUPABASE_SERVICE_ROLE=
